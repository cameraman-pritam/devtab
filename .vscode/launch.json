{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "Bun: Build DevTab",
      "command": "bun run build",
      "cwd": "${workspaceFolder}/source"
    }
  ]
}
